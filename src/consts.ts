
/* CONSTS */

const EMPTY_ARRAY = [];

const SELECTOR_IDENTITY = ( ...args ) => args.length > 1 ? args : args[0];

const NOOP = () => {};

/* EXPORT */

export {EMPTY_ARRAY, SELECTOR_IDENTITY, NOOP};
